# Required for FLUX.2 cloud mode on your own computer. Never commit real values.
FAL_KEY=
# Choose a long local password so only your browser can request short-lived tokens.
SURFACESHIFT_ACCESS_CODE=

# Optional Apple Silicon fallback: run ./setup_mac.sh once, then ./run_mac.sh.
